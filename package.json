{
  "name": "ekovits-invoice-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix frontend",
    "db:setup": "npm run db:setup --prefix server",
    "server:dev": "npm run dev --prefix server",
    "client:dev": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
    "test": "npm run test --prefix server && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
